db:
  host: localhost
  username: root
  password: root
  name: db-local
  port: 3306
  url: jdbc:mysql://${db.host}:${db.port}/${db.name}

spring.jpa.hibernate.ddl-auto: create

redis-config:
  host: localhost